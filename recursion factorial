#include<stdio.h>
int fac(int);
int main()
{
  int n;
  printf("enter the number : \n");
  scanf("%d",&n);
  printf("%d factorial is %d",n,fac(n));	
}
int fac(int n)
{
	if(n==1)
	 return 1;
	 else
	 return (n*fac(n-1));
}
